"""Обработчики информационных разделов"""
from aiogram import Router, F
from aiogram.types import CallbackQuery
from config import OWNERS_CHAT_LINK

router = Router()


@router.callback_query(F.data == "how_it_works")
async def how_it_works(callback: CallbackQuery):
    """Обработчик кнопки 'Как это работает'"""
    await callback.answer()
    
    text = """Лови демо! 👇

[Здесь вставляется GIF или видео]
(Видео: 1) Копирую ссылку из подозрительного сообщения в Telegram 2) Вставляю в браузер 3) Навожу курсор на ссылку в адресной строке 4) СРАЗУ всплывает красное предупреждение: «SCAM! Фишинг!»)

Видишь? Никаких лишних действий. Не нужно ничего открывать, копировать, проверять вручную. Навел курсор — сразу видишь вердикт.

Это работает потому что:
✅ У нас своя постоянно обновляемая база угроз
✅ Каждый пользователь анонимно пополняет её (если нашел новую угрозу)
✅ Проверка идет локально, твои данные никуда не утекают

И самое главное — это работает ДО того, как ты кликнул по ссылке и подключил кошелек. После клика может быть уже поздно.

Хочешь такую защиту?"""
    
    from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton
    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(text="🛒 Да, хочу! Купить за 500₽", callback_data="buy_license")],
        [InlineKeyboardButton(text="← Назад в меню", callback_data="main_menu")]
    ])
    
    await callback.message.edit_text(text, reply_markup=keyboard)


@router.callback_query(F.data == "faq")
async def faq(callback: CallbackQuery):
    """Обработчик FAQ"""
    await callback.answer()
    
    text = """Собрал тут вопросы, которые задают чаще всего:

❔ **«Чем это лучше обычного антивируса?»**
Антивирус проверяет файлы, мы проверяем ссылки. Это разные вещи. Большинство антивирусов НЕ проверяют ссылки на фишинг в реальном времени. Мы — проверяем.

❔ **«А если я уже купил, а вы завтра закроетесь?»**
Ключ работает локально в расширении. Даже если мы закроемся, твоя версия будет работать. Но мы не планируем закрываться 😉

❔ **«На каких браузерах работает?»**
Сейчас: Chrome, Edge, Brave. В течение месяца добавим Firefox.

❔ **«Как пополняется база угроз?»**
Анонимно. Если любой пользователь AEGIS встречает новую угрозу — она добавляется в общую базу. Так защита становится умнее каждый день.

❔ **«Можно купить нескольким людям?»**
Да, можно. Просто каждый должен пройти процесс оплаты и получить свой уникальный ключ.

❔ **«Есть гарантия?»**
Да. Если в течение 7 дней после покупки что-то не работает — пиши, вернем деньги.

Остались вопросы? Пиши сюда 👇"""
    
    from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton
    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(text="👨💻 Написать в поддержку", callback_data="support")],
        [InlineKeyboardButton(text="🛒 Все равно хочу купить", callback_data="buy_license")],
        [InlineKeyboardButton(text="← Назад", callback_data="main_menu")]
    ])
    
    await callback.message.edit_text(text, reply_markup=keyboard, parse_mode="Markdown")


@router.callback_query(F.data == "support")
async def support(callback: CallbackQuery):
    """Обработчик поддержки"""
    await callback.answer()
    
    text = """По всем вопросам:

• **По ключам и активации:** @aegis_support_bot (наш авто-помощник)
• **По оплате и возвратам:** @aegis_finance (менеджер по платежам)
• **По сотрудничеству и рекламе:** @aegis_partners
• **Экстренная связь** (если все остальные не отвечают 12+ часов): @aegis_ceo

Обычно отвечаем в течение 15-30 минут с 10:00 до 22:00 по МСК.

📌 **Прежде чем писать:**
1. Готовь свой ID: {user_id} (цифры из ссылки t.me/user?id=...)
2. Если проблема с ключом — подготовь сам ключ
3. Если проблема с оплатой — скриншот чека

Так мы поможем тебе быстрее!""".format(user_id=callback.from_user.id)
    
    from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton
    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(text="🏠 В главное меню", callback_data="main_menu")],
        [InlineKeyboardButton(text="❓ Частые вопросы", callback_data="faq")]
    ])
    
    await callback.message.edit_text(text, reply_markup=keyboard, parse_mode="Markdown")


@router.callback_query(F.data == "help")
async def help_handler(callback: CallbackQuery):
    """Обработчик помощи"""
    await callback.answer()
    
    text = """Нужна помощь? Вот что можно сделать:

📋 **Инструкция по установке:**
1. Перейди в Chrome Web Store
2. Найди расширение AEGIS
3. Установи его
4. Открой расширение и введи свой ключ

🔑 **Если потерял ключ:**
Используй команду /start — там будет твой ключ

❓ **Другие вопросы:**
Напиши в поддержку — мы поможем!"""
    
    from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton
    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(text="👨💻 Написать в поддержку", callback_data="support")],
        [InlineKeyboardButton(text="🏠 В главное меню", callback_data="main_menu")]
    ])
    
    await callback.message.edit_text(text, reply_markup=keyboard)

